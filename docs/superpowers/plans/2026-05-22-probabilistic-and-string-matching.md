# Spec 3 — Probabilistic Structures + String Matching Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add 5 methods to dsvisual — Bloom Filter, Skip List, Count-Min Sketch (Hash & Probabilistic) and Z-Algorithm, Aho-Corasick (Searching & String Matching) — each with a C++ source, a dynamic visualizer, a bilingual slide deck, and a Playwright test.

**Architecture:** Each method appends an entry to an existing `METHOD_GROUPS` group (no new category, no `index.html` changes). Every visualizer renders through `acquireDynamicVizHost()` into `#dynamic-viz-host` and embeds its own controls. The three stepped visualizers (Skip List search, Z-Algorithm, Aho-Corasick) share one small `buildStepControls()` helper. Slides are authored in `slides_db.js` then generated by `npm run build:slides`.

**Tech Stack:** Vanilla browser JS (no build step, opened via `file://`), CommonJS Node scripts (`build_db.js`, `build_slides.js`), Playwright + `node:test`, Marp slide pipeline, clang-format via `npm run format:code`.

---

## File Structure

**Created:**
- `bloom_filter.cpp`, `skip_list.cpp`, `count_min_sketch.cpp`, `search_zalgo.cpp`, `search_aho.cpp` — the 5 C++ teaching sources.

**Modified:**
- `build_db.js` — 5 new `.cpp → code constant` mappings (Task 1).
- `code_db.js` — regenerated by `node build_db.js` (never hand-edited).
- `app.js` — `METHOD_GROUPS` entries, `getCodeForMethod` mappings, `updateLayout` branches, `renderAll` dispatch, `buildStepControls` helper + 5 render functions.
- `index.html` — **no changes** (controls are embedded by the render functions).
- `style.css` — CSS appended at end, namespaced `bloom-` / `skiplist-` / `cms-` / `zalgo-` / `aho-` / `stepctl`.
- `slides_db.js` — 5 new decks; `slides_rendered.js` + `slides/` regenerated by `npm run build:slides`.
- `tests/visualizer.spec.js` — 5 new render tests + 2 new paths in the existing dynamic→static regression test.

**Reference patterns to mirror (read before starting):**
- `acquireDynamicVizHost()` — `app.js:2678`.
- `renderDeque()` (interactive, immediate redraw) — `app.js:2900`.
- `renderKMP()` (stepped Step/Run/Reset) — `app.js:2954`.
- `renderAll()` dispatch — `app.js:1940`.
- `updateLayout()` `search-*` / `deque` branches — `app.js:1762-1785`.
- `getCodeForMethod()` — `app.js:255`.
- A complete deck (`"deque"`) — `slides_db.js:4256`.

---

## Task 1: Foundation — C++ sources + build_db mappings

**Files:**
- Modify: `build_db.js:67-72` (mappings table)
- Create: `bloom_filter.cpp`, `skip_list.cpp`, `count_min_sketch.cpp`, `search_zalgo.cpp`, `search_aho.cpp`
- Regenerate: `code_db.js`

- [ ] **Step 1: Add the 5 build_db.js mappings**

In `build_db.js`, the `mappings` object ends at line 71 (`'search_strcompare.cpp': 'codeSearchStrCompare'`). Add a comma after it and append 5 entries so the object reads:

```js
    'search_strcompare.cpp': 'codeSearchStrCompare',
    'bloom_filter.cpp': 'codeBloomFilter',
    'skip_list.cpp': 'codeSkipList',
    'count_min_sketch.cpp': 'codeCountMinSketch',
    'search_zalgo.cpp': 'codeSearchZAlgo',
    'search_aho.cpp': 'codeSearchAho'
};
```

This must happen **before** the `.cpp` files exist — `code_db.js` regeneration silently drops any constant with no mapping (PR #57 lesson).

- [ ] **Step 2: Create `bloom_filter.cpp`**

```cpp
#include <iostream>
#include <vector>
#include <string>

// A Bloom filter: a space-efficient probabilistic set. A query may report a
// false positive ("possibly present") but never a false negative.
class BloomFilter {
    static const int SIZE = 32;
    std::vector<bool> bits;

    int hash1(const std::string& s) const {
        unsigned long h = 5381;
        for (char c : s) h = h * 33 + static_cast<unsigned char>(c);
        return static_cast<int>(h % SIZE);
    }
    int hash2(const std::string& s) const {
        unsigned long h = 0;
        for (char c : s) h = h * 31 + static_cast<unsigned char>(c);
        return static_cast<int>(h % SIZE);
    }
    int hash3(const std::string& s) const {
        unsigned long h = 7;
        for (char c : s) h = h * 17 + static_cast<unsigned char>(c) + 1;
        return static_cast<int>(h % SIZE);
    }

public:
    BloomFilter() : bits(SIZE, false) {}

    void insert(const std::string& key) {
        bits[hash1(key)] = true;
        bits[hash2(key)] = true;
        bits[hash3(key)] = true;
    }

    bool possiblyContains(const std::string& key) const {
        return bits[hash1(key)] && bits[hash2(key)] && bits[hash3(key)];
    }
};

int main() {
    BloomFilter bf;
    bf.insert("cat");
    bf.insert("dog");
    bf.insert("bird");

    // "dog" was inserted, so this is always true.
    std::cout << "contains dog?  " << bf.possiblyContains("dog") << "\n";
    // "fish" was never inserted: 0 means definitely absent, 1 a false positive.
    std::cout << "contains fish? " << bf.possiblyContains("fish") << "\n";
    return 0;
}
```

- [ ] **Step 3: Create `skip_list.cpp`**

```cpp
#include <iostream>
#include <vector>
#include <cstdlib>

// A skip list: an ordered map built from a multi-level linked list. Each node
// is promoted to a random number of express lanes, giving expected O(log n).
struct Node {
    int key;
    std::vector<Node*> forward;
    Node(int k, int level) : key(k), forward(level + 1, nullptr) {}
};

class SkipList {
    static const int MAX_LEVEL = 3;  // levels 0..3
    Node* head;
    int level;

    int randomLevel() {
        int lvl = 0;
        while ((std::rand() & 1) && lvl < MAX_LEVEL) lvl++;
        return lvl;
    }

public:
    SkipList() : level(0) { head = new Node(-1, MAX_LEVEL); }

    void insert(int key) {
        std::vector<Node*> update(MAX_LEVEL + 1, head);
        Node* cur = head;
        for (int i = level; i >= 0; i--) {
            while (cur->forward[i] && cur->forward[i]->key < key) cur = cur->forward[i];
            update[i] = cur;
        }
        int lvl = randomLevel();
        if (lvl > level) {
            for (int i = level + 1; i <= lvl; i++) update[i] = head;
            level = lvl;
        }
        Node* node = new Node(key, lvl);
        for (int i = 0; i <= lvl; i++) {
            node->forward[i] = update[i]->forward[i];
            update[i]->forward[i] = node;
        }
    }

    bool search(int key) {
        Node* cur = head;
        for (int i = level; i >= 0; i--) {
            while (cur->forward[i] && cur->forward[i]->key < key) cur = cur->forward[i];
        }
        cur = cur->forward[0];
        return cur && cur->key == key;
    }

    void remove(int key) {
        std::vector<Node*> update(MAX_LEVEL + 1, head);
        Node* cur = head;
        for (int i = level; i >= 0; i--) {
            while (cur->forward[i] && cur->forward[i]->key < key) cur = cur->forward[i];
            update[i] = cur;
        }
        cur = cur->forward[0];
        if (!cur || cur->key != key) return;
        for (int i = 0; i <= level; i++) {
            if (update[i]->forward[i] != cur) break;
            update[i]->forward[i] = cur->forward[i];
        }
        delete cur;
        while (level > 0 && !head->forward[level]) level--;
    }
};

int main() {
    SkipList sl;
    int keys[] = {3, 7, 12, 19, 25};
    for (int k : keys) sl.insert(k);

    std::cout << "search 12? " << sl.search(12) << "\n";  // 1
    std::cout << "search 20? " << sl.search(20) << "\n";  // 0
    sl.remove(12);
    std::cout << "search 12 after remove? " << sl.search(12) << "\n";  // 0
    return 0;
}
```

- [ ] **Step 4: Create `count_min_sketch.cpp`**

```cpp
#include <iostream>
#include <string>
#include <algorithm>

// A Count-Min Sketch: a probabilistic frequency table. estimate() never
// underestimates a count; hash collisions may inflate it.
class CountMinSketch {
    static const int DEPTH = 3;
    static const int WIDTH = 8;
    int table[DEPTH][WIDTH];

    int hash(int row, const std::string& s) const {
        unsigned long h = static_cast<unsigned long>(row + 1) * 2654435761UL;
        for (char c : s) h = h * 31 + static_cast<unsigned char>(c);
        return static_cast<int>(h % WIDTH);
    }

public:
    CountMinSketch() {
        for (int r = 0; r < DEPTH; r++)
            for (int c = 0; c < WIDTH; c++) table[r][c] = 0;
    }

    void update(const std::string& key) {
        for (int r = 0; r < DEPTH; r++) table[r][hash(r, key)]++;
    }

    int estimate(const std::string& key) const {
        int est = table[0][hash(0, key)];
        for (int r = 1; r < DEPTH; r++) est = std::min(est, table[r][hash(r, key)]);
        return est;
    }
};

int main() {
    CountMinSketch cms;
    for (int i = 0; i < 5; i++) cms.update("apple");
    for (int i = 0; i < 2; i++) cms.update("banana");
    cms.update("cherry");

    std::cout << "apple  ~ " << cms.estimate("apple") << "\n";   // >= 5
    std::cout << "banana ~ " << cms.estimate("banana") << "\n";  // >= 2
    std::cout << "grape  ~ " << cms.estimate("grape") << "\n";   // >= 0
    return 0;
}
```

- [ ] **Step 5: Create `search_zalgo.cpp`**

```cpp
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

// The Z-array: Z[i] is the length of the longest substring starting at i that
// matches a prefix of the string. Built in linear time with a [l, r] window.
std::vector<int> computeZ(const std::string& s) {
    int n = static_cast<int>(s.size());
    std::vector<int> z(n, 0);
    int l = 0, r = 0;
    for (int i = 1; i < n; i++) {
        if (i < r) z[i] = std::min(r - i, z[i - l]);
        while (i + z[i] < n && s[z[i]] == s[i + z[i]]) z[i]++;
        if (i + z[i] > r) { l = i; r = i + z[i]; }
    }
    return z;
}

// String matching: build pattern + '$' + text; an index whose Z-value equals
// the pattern length marks an occurrence.
std::vector<int> zSearch(const std::string& text, const std::string& pattern) {
    std::string combined = pattern + "$" + text;
    std::vector<int> z = computeZ(combined);
    std::vector<int> matches;
    int m = static_cast<int>(pattern.size());
    for (int i = 0; i < static_cast<int>(combined.size()); i++) {
        if (z[i] == m) matches.push_back(i - m - 1);  // translate back into text
    }
    return matches;
}

int main() {
    std::string text = "ABABDABACDABABCABAB";
    std::string pattern = "ABABCABAB";
    std::vector<int> matches = zSearch(text, pattern);

    std::cout << "matches at:";
    for (int idx : matches) std::cout << " " << idx;
    std::cout << "\n";
    return 0;
}
```

- [ ] **Step 6: Create `search_aho.cpp`**

```cpp
#include <iostream>
#include <map>
#include <queue>
#include <vector>
#include <string>

// Aho-Corasick: a trie of all patterns plus BFS-computed failure links, so a
// single text scan reports every occurrence of every pattern.
struct Node {
    std::map<char, Node*> children;
    Node* fail = nullptr;
    std::vector<int> output;
};

class AhoCorasick {
    Node* root;
    std::vector<std::string> patterns;

public:
    AhoCorasick() { root = new Node(); }

    void addPattern(const std::string& p) {
        int idx = static_cast<int>(patterns.size());
        patterns.push_back(p);
        Node* cur = root;
        for (char c : p) {
            if (!cur->children.count(c)) cur->children[c] = new Node();
            cur = cur->children[c];
        }
        cur->output.push_back(idx);
    }

    void build() {
        std::queue<Node*> q;
        root->fail = root;
        for (auto& kv : root->children) {
            kv.second->fail = root;
            q.push(kv.second);
        }
        while (!q.empty()) {
            Node* cur = q.front();
            q.pop();
            for (auto& kv : cur->children) {
                char c = kv.first;
                Node* child = kv.second;
                Node* f = cur->fail;
                while (f != root && !f->children.count(c)) f = f->fail;
                if (f->children.count(c) && f->children[c] != child)
                    child->fail = f->children[c];
                else
                    child->fail = root;
                for (int idx : child->fail->output) child->output.push_back(idx);
                q.push(child);
            }
        }
    }

    void search(const std::string& text) {
        Node* cur = root;
        for (int i = 0; i < static_cast<int>(text.size()); i++) {
            char c = text[i];
            while (cur != root && !cur->children.count(c)) cur = cur->fail;
            if (cur->children.count(c)) cur = cur->children[c];
            for (int idx : cur->output) {
                int start = i - static_cast<int>(patterns[idx].size()) + 1;
                std::cout << "match \"" << patterns[idx] << "\" at " << start << "\n";
            }
        }
    }
};

int main() {
    AhoCorasick ac;
    ac.addPattern("he");
    ac.addPattern("she");
    ac.addPattern("his");
    ac.addPattern("hers");
    ac.build();
    ac.search("ushers");  // she@1, he@2, hers@2
    return 0;
}
```

- [ ] **Step 7: Verify all 5 compile**

Run: `for f in bloom_filter skip_list count_min_sketch search_zalgo search_aho; do g++ -std=c++17 -fsyntax-only $f.cpp && echo "$f OK"; done`
Expected: five `OK` lines, no errors.

- [ ] **Step 8: Regenerate `code_db.js`**

Run: `node build_db.js`
Then: `grep -c "codeBloomFilter\|codeSkipList\|codeCountMinSketch\|codeSearchZAlgo\|codeSearchAho" code_db.js`
Expected: `5` (one `const` definition for each).

- [ ] **Step 9: Commit**

```bash
git add build_db.js bloom_filter.cpp skip_list.cpp count_min_sketch.cpp search_zalgo.cpp search_aho.cpp code_db.js
git commit -m "feat: add 5 probabilistic + string-matching C++ sources + build_db mappings"
```

---

## Task 2: bloom-filter method

**Files:**
- Modify: `app.js` — `METHOD_GROUPS` `hash` group, `getCodeForMethod`, `updateLayout`, `renderAll`, new `renderBloomFilter()`
- Modify: `style.css` — append `bloom-` rules
- Test: `tests/visualizer.spec.js`

- [ ] **Step 1: Write the failing Playwright test**

Add this test in `tests/visualizer.spec.js` immediately after the `String: String Matching Compared` test (before the `Navigation:` regression test):

```js
    test('Probabilistic: Bloom Filter renders a 32-bit array and supports insert/query', async ({ page }) => {
        await loadMethod(page, 'bloom-filter');
        const card = page.locator('[data-method-section="bloom-filter"]');
        await expect(card.locator('.code-panel-filename')).toContainText('bloom_filter.cpp');
        await expect(card.locator('.bloom-cell')).toHaveCount(32);
        await expect(card.locator('.bloom-cell.bloom-on').first()).toBeVisible();
        await card.locator('[data-bloom-val]').fill('zebra');
        await card.locator('[data-action="bloom-insert"]').click();
        await card.locator('[data-action="bloom-query"]').click();
        await expect(card.locator('[data-testid="bloom-hashes"]')).toContainText('zebra');
    });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx playwright test -g "Bloom Filter renders"`
Expected: FAIL — `Method bloom-filter not found in method dropdown`.

- [ ] **Step 3: Add the `METHOD_GROUPS` entry**

In `app.js`, the `hash` group (`id: 'hash'`, `app.js:140-147`) ends its `methods` array with the `hash-bucket` line. Append one entry:

```js
            { id: 'hash-bucket', title: 'Bucketing', file: 'hash_bucket.cpp', visualizer: 'hash', controls: 'hash' },
            { id: 'bloom-filter', title: 'Bloom Filter', file: 'bloom_filter.cpp', visualizer: 'bloom', controls: 'bloom' },
```

(`skip-list` and `count-min-sketch` are appended by Tasks 3 and 4. The `visualizer`/`controls` fields are descriptive metadata only.)

- [ ] **Step 4: Add the `getCodeForMethod` mapping**

In `app.js`, the `codeByMethod` object has `'hash-bucket': codeHashBucket,` (`app.js:283`). Add one line after it:

```js
        'hash-bucket': codeHashBucket,
        'bloom-filter': codeBloomFilter,
```

- [ ] **Step 5: Add the `updateLayout` branch**

In `app.js`, after the `deque` branch (`app.js:1782-1785`, which ends `}`) and before `else if (currentMode.includes('hash-'))`, insert:

```js
        else if (currentMode === 'bloom-filter') {
            codeTitle.textContent = 'bloom_filter.cpp';
            codeDisplay.textContent = codeBloomFilter;
        }
```

The id does not contain `hash-`, so `currentMode.includes('hash-')` will not capture it, but keeping the branch adjacent is clearer.

- [ ] **Step 6: Add the `renderAll` dispatch**

In `app.js` `renderAll()`, after `else if (currentMode === 'deque') renderDeque();` (`app.js:1943`), insert:

```js
        else if (currentMode === 'bloom-filter') renderBloomFilter();
```

- [ ] **Step 7: Add the `renderBloomFilter` function**

In `app.js`, immediately after the `renderDeque()` function (it ends at `app.js:2952`), add:

```js
    function renderBloomFilter() {
        const host = acquireDynamicVizHost();
        const SIZE = 32;
        function h1(s) { let h = 5381; for (const c of s) h = (h * 33 + c.charCodeAt(0)) >>> 0; return h % SIZE; }
        function h2(s) { let h = 0; for (const c of s) h = (h * 31 + c.charCodeAt(0)) >>> 0; return h % SIZE; }
        function h3(s) { let h = 7; for (const c of s) h = (h * 17 + c.charCodeAt(0) + 1) >>> 0; return h % SIZE; }
        function hashes(s) { return [h1(s), h2(s), h3(s)]; }

        if (!Array.isArray(runtimeVisualizer._bloomBits)) {
            runtimeVisualizer._bloomBits = new Array(SIZE).fill(false);
            runtimeVisualizer._bloomItems = [];
            for (const w of ['cat', 'dog', 'bird']) {
                for (const i of hashes(w)) runtimeVisualizer._bloomBits[i] = true;
                runtimeVisualizer._bloomItems.push(w);
            }
        }
        const bits = runtimeVisualizer._bloomBits;
        const items = runtimeVisualizer._bloomItems;

        const wrap = document.createElement('div');
        wrap.className = 'bloom-wrap';
        let html = '<div class="bloom-row">';
        for (let i = 0; i < SIZE; i++) {
            html += '<span class="bloom-cell' + (bits[i] ? ' bloom-on' : '') +
                    '" data-bit="' + i + '">' + (bits[i] ? 1 : 0) + '</span>';
        }
        html += '</div>';
        html += '<div class="bloom-hashes" data-testid="bloom-hashes"></div>';
        html += '<div class="bloom-items"><strong>inserted:</strong> <span class="bloom-items-list">' +
                items.join(', ') + '</span></div>';
        html += '<div class="bloom-controls" role="group">' +
                    '<input type="text" value="fish" data-bloom-val>' +
                    '<button type="button" data-action="bloom-insert">Insert</button>' +
                    '<button type="button" data-action="bloom-query">Query</button>' +
                '</div>';
        wrap.innerHTML = html;
        host.appendChild(wrap);

        const valInput = wrap.querySelector('[data-bloom-val]');
        const hashesEl = wrap.querySelector('.bloom-hashes');
        function highlight(idxs, cls) {
            wrap.querySelectorAll('.bloom-cell').forEach((c) => c.classList.remove('bloom-hit', 'bloom-miss'));
            for (const i of idxs) {
                const cell = wrap.querySelector('.bloom-cell[data-bit="' + i + '"]');
                if (cell) cell.classList.add(cls);
            }
        }
        wrap.querySelector('[data-action="bloom-insert"]').onclick = () => {
            const key = valInput.value.trim();
            if (!key) { showStatus('Enter a word', '#f87171'); return; }
            const idxs = hashes(key);
            for (const i of idxs) bits[i] = true;
            if (!items.includes(key)) items.push(key);
            renderBloomFilter();
            showStatus('Inserted "' + key + '" → bits {' + idxs.join(', ') + '}', '#34d399');
        };
        wrap.querySelector('[data-action="bloom-query"]').onclick = () => {
            const key = valInput.value.trim();
            if (!key) { showStatus('Enter a word', '#f87171'); return; }
            const idxs = hashes(key);
            hashesEl.textContent = 'hashes of "' + key + '" → {' + idxs.join(', ') + '}';
            const present = idxs.every((i) => bits[i]);
            highlight(idxs, present ? 'bloom-hit' : 'bloom-miss');
            if (present) showStatus('"' + key + '" possibly present', '#f59e0b');
            else showStatus('"' + key + '" definitely not present', '#60a5fa');
        };
    }
```

- [ ] **Step 8: Append the `bloom-` CSS**

At the end of `style.css`, append:

```css
/* Spec 3 — Bloom Filter */
.bloom-wrap { display: flex; flex-direction: column; gap: 10px; align-items: center; }
.bloom-row { display: flex; flex-wrap: wrap; gap: 3px; justify-content: center; }
.bloom-cell { width: 22px; height: 26px; display: flex; align-items: center; justify-content: center;
  border: 1px solid #cbd5e1; border-radius: 3px; font-family: monospace; font-size: 12px; background: #f8fafc; }
.bloom-cell.bloom-on { background: #dbeafe; border-color: #3b82f6; font-weight: 700; }
.bloom-cell.bloom-hit { background: #fde68a; border-color: #f59e0b; }
.bloom-cell.bloom-miss { background: #fecaca; border-color: #ef4444; }
.bloom-hashes { font-family: monospace; font-size: 13px; color: #475569; min-height: 1.2em; }
.bloom-items { font-size: 13px; color: #475569; }
.bloom-controls { display: flex; gap: 6px; flex-wrap: wrap; justify-content: center; }
```

- [ ] **Step 9: Run the test to verify it passes**

Run: `npx playwright test -g "Bloom Filter renders"`
Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add app.js style.css tests/visualizer.spec.js
git commit -m "feat: bloom-filter probabilistic method"
```

---

## Task 3: skip-list method (introduces `buildStepControls`)

**Files:**
- Modify: `app.js` — `METHOD_GROUPS` `hash` group, `getCodeForMethod`, `updateLayout`, `renderAll`, new `buildStepControls()` + `renderSkipList()`
- Modify: `style.css` — append `skiplist-` and `stepctl` rules
- Test: `tests/visualizer.spec.js`

- [ ] **Step 1: Write the failing Playwright test**

Add after the `Bloom Filter renders` test:

```js
    test('Probabilistic: Skip List renders 4 levels and supports insert + stepped search', async ({ page }) => {
        await loadMethod(page, 'skip-list');
        const card = page.locator('[data-method-section="skip-list"]');
        await expect(card.locator('.code-panel-filename')).toContainText('skip_list.cpp');
        await expect(card.locator('.skiplist-level')).toHaveCount(4);
        const l0 = card.locator('.skiplist-level[data-level="0"] .skiplist-node:not(.skiplist-ghost)');
        await expect(l0).toHaveCount(5);
        await card.locator('[data-skiplist-val]').fill('15');
        await card.locator('[data-action="skiplist-insert"]').click();
        await expect(l0).toHaveCount(6);
        await card.locator('[data-skiplist-search]').fill('12');
        await card.locator('[data-action="step"]').click();
        await expect(card.locator('[data-testid="skiplist-status"]')).toBeVisible();
    });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx playwright test -g "Skip List renders"`
Expected: FAIL — `Method skip-list not found in method dropdown`.

- [ ] **Step 3: Add the `METHOD_GROUPS` entry**

In `app.js`, the `hash` group's `methods` array now ends with the `bloom-filter` line (added in Task 2). Append one entry:

```js
            { id: 'bloom-filter', title: 'Bloom Filter', file: 'bloom_filter.cpp', visualizer: 'bloom', controls: 'bloom' },
            { id: 'skip-list', title: 'Skip List', file: 'skip_list.cpp', visualizer: 'skiplist', controls: 'skiplist' },
```

- [ ] **Step 4: Add the `getCodeForMethod` mapping**

In `app.js`, after `'bloom-filter': codeBloomFilter,`, add:

```js
        'bloom-filter': codeBloomFilter,
        'skip-list': codeSkipList,
```

- [ ] **Step 5: Add the `updateLayout` branch**

In `app.js`, after the `bloom-filter` branch added in Task 2, insert:

```js
        else if (currentMode === 'skip-list') {
            codeTitle.textContent = 'skip_list.cpp';
            codeDisplay.textContent = codeSkipList;
        }
```

- [ ] **Step 6: Add the `renderAll` dispatch**

In `app.js` `renderAll()`, after the `bloom-filter` dispatch line added in Task 2, insert:

```js
        else if (currentMode === 'skip-list') renderSkipList();
```

- [ ] **Step 7: Add the `buildStepControls` helper**

In `app.js`, immediately before the `renderBloomFilter()` function added in Task 2, add the shared helper:

```js
    function buildStepControls(onStep, onReset, runIntervalMs) {
        const strip = document.createElement('div');
        strip.className = 'stepctl';
        strip.innerHTML =
            '<button type="button" data-action="step">Step</button>' +
            '<button type="button" data-action="run">Run</button>' +
            '<button type="button" data-action="reset">Reset</button>';
        let runTimer = null;
        strip.querySelector('[data-action="step"]').onclick = () => { onStep(); };
        strip.querySelector('[data-action="run"]').onclick = () => {
            if (runTimer) return;
            runTimer = setInterval(() => {
                const more = onStep();
                if (more === false) { clearInterval(runTimer); runTimer = null; }
            }, runIntervalMs || 500);
        };
        strip.querySelector('[data-action="reset"]').onclick = () => {
            if (runTimer) { clearInterval(runTimer); runTimer = null; }
            onReset();
        };
        return strip;
    }
```

`onStep` returns `false` when no steps remain, which stops `Run`. This helper is reused by Tasks 5 and 6.

- [ ] **Step 8: Add the `renderSkipList` function**

In `app.js`, immediately after the `renderBloomFilter()` function, add:

```js
    function renderSkipList() {
        const host = acquireDynamicVizHost();
        const MAXLVL = 4;  // levels 0..3
        if (!runtimeVisualizer._skipList) {
            runtimeVisualizer._skipList = {
                nodes: [
                    { key: 3, h: 1 }, { key: 7, h: 2 }, { key: 12, h: 3 },
                    { key: 19, h: 1 }, { key: 25, h: 1 },
                ],
            };
        }
        const sl = runtimeVisualizer._skipList;
        sl.nodes.sort((a, b) => a.key - b.key);

        function randomLevel() {
            let lvl = 1;
            while (Math.random() < 0.5 && lvl < MAXLVL) lvl++;
            return lvl;
        }
        function computePath(target) {
            const path = [];
            let level = MAXLVL - 1, idx = -1;
            while (level >= 0) {
                let next = idx + 1;
                while (next < sl.nodes.length && sl.nodes[next].h <= level) next++;
                if (next < sl.nodes.length && sl.nodes[next].key < target) {
                    idx = next;
                    path.push({ level: level, idx: idx, kind: 'right' });
                } else {
                    path.push({ level: level, idx: idx, kind: 'down' });
                    level--;
                }
            }
            const after = idx + 1;
            if (after < sl.nodes.length && sl.nodes[after].key === target) {
                path.push({ level: 0, idx: after, kind: 'found' });
            } else {
                path.push({ level: 0, idx: idx, kind: 'notfound' });
            }
            return path;
        }

        let searchPath = null, searchStep = 0;

        const wrap = document.createElement('div');
        wrap.className = 'skiplist-wrap';
        wrap.innerHTML =
            '<div class="skiplist-grid"></div>' +
            '<div class="skiplist-status" data-testid="skiplist-status">&nbsp;</div>' +
            '<div class="skiplist-controls" role="group">' +
                '<input type="number" value="15" data-skiplist-val>' +
                '<button type="button" data-action="skiplist-insert">Insert</button>' +
                '<button type="button" data-action="skiplist-delete">Delete</button>' +
                '<input type="number" value="12" data-skiplist-search>' +
            '</div>';
        host.appendChild(wrap);
        const gridEl = wrap.querySelector('.skiplist-grid');
        const statusEl = wrap.querySelector('.skiplist-status');

        function activeStep() {
            if (!searchPath || searchStep === 0) return null;
            return searchPath[searchStep - 1];
        }
        function draw() {
            const act = activeStep();
            let html = '';
            for (let L = MAXLVL - 1; L >= 0; L--) {
                html += '<div class="skiplist-level" data-level="' + L + '">';
                html += '<span class="skiplist-head' +
                        (act && act.level === L && act.idx === -1 ? ' skiplist-active' : '') +
                        '">head</span>';
                for (let i = 0; i < sl.nodes.length; i++) {
                    const n = sl.nodes[i];
                    if (n.h > L) {
                        html += '<span class="skiplist-arrow">&rarr;</span>';
                        const on = act && act.level === L && act.idx === i;
                        html += '<span class="skiplist-node' + (on ? ' skiplist-active' : '') +
                                '" data-key="' + n.key + '">' + n.key + '</span>';
                    } else {
                        html += '<span class="skiplist-arrow skiplist-skip">&middot;&middot;&middot;</span>';
                        html += '<span class="skiplist-node skiplist-ghost"></span>';
                    }
                }
                html += '<span class="skiplist-arrow">&rarr;</span><span class="skiplist-nil">NIL</span>';
                html += '</div>';
            }
            gridEl.innerHTML = html;
        }
        function resetSearch() {
            searchPath = null;
            searchStep = 0;
            statusEl.innerHTML = '&nbsp;';
            draw();
        }
        function stepSearch() {
            const target = parseInt(wrap.querySelector('[data-skiplist-search]').value, 10);
            if (Number.isNaN(target)) { showStatus('Enter a search key', '#f87171'); return false; }
            if (!searchPath) { searchPath = computePath(target); searchStep = 0; }
            if (searchStep >= searchPath.length) return false;
            const s = searchPath[searchStep];
            searchStep++;
            draw();
            if (s.kind === 'found') statusEl.textContent = 'Found ' + target;
            else if (s.kind === 'notfound') statusEl.textContent = target + ' not found';
            else statusEl.textContent = 'level ' + s.level + ': move ' + s.kind;
            return searchStep < searchPath.length;
        }

        wrap.querySelector('[data-action="skiplist-insert"]').onclick = () => {
            const v = parseInt(wrap.querySelector('[data-skiplist-val]').value, 10);
            if (Number.isNaN(v)) { showStatus('Enter a number', '#f87171'); return; }
            if (sl.nodes.some((n) => n.key === v)) { showStatus(v + ' already present', '#f87171'); return; }
            const h = randomLevel();
            sl.nodes.push({ key: v, h: h });
            showStatus('Inserted ' + v + ' (level ' + h + ')', '#34d399');
            renderSkipList();
        };
        wrap.querySelector('[data-action="skiplist-delete"]').onclick = () => {
            const v = parseInt(wrap.querySelector('[data-skiplist-val]').value, 10);
            if (Number.isNaN(v)) { showStatus('Enter a number', '#f87171'); return; }
            const before = sl.nodes.length;
            sl.nodes = sl.nodes.filter((n) => n.key !== v);
            if (sl.nodes.length === before) showStatus(v + ' not found', '#f87171');
            else showStatus('Deleted ' + v, '#60a5fa');
            renderSkipList();
        };

        wrap.appendChild(buildStepControls(stepSearch, resetSearch, 500));
        draw();
    }
```

- [ ] **Step 9: Append the `skiplist-` and `stepctl` CSS**

At the end of `style.css`, append:

```css
/* Spec 3 — shared step controls */
.stepctl { display: flex; gap: 6px; flex-wrap: wrap; justify-content: center; margin-top: 6px; }

/* Spec 3 — Skip List */
.skiplist-wrap { display: flex; flex-direction: column; gap: 10px; }
.skiplist-grid { display: flex; flex-direction: column; gap: 4px; overflow-x: auto; }
.skiplist-level { display: flex; align-items: center; gap: 2px; }
.skiplist-head, .skiplist-nil { font-family: monospace; font-size: 11px; color: #64748b; padding: 2px 4px; }
.skiplist-head.skiplist-active { background: #34d399; color: #fff; border-radius: 3px; }
.skiplist-node { min-width: 34px; height: 28px; display: flex; align-items: center; justify-content: center;
  border: 1px solid #3b82f6; border-radius: 4px; background: #eff6ff; font-family: monospace; font-weight: 700; }
.skiplist-node.skiplist-ghost { border: 1px dashed #e2e8f0; background: transparent; }
.skiplist-node.skiplist-active { background: #34d399; border-color: #059669; color: #fff; }
.skiplist-arrow { color: #94a3b8; font-family: monospace; }
.skiplist-arrow.skiplist-skip { color: #cbd5e1; }
.skiplist-status { font-family: monospace; font-size: 13px; color: #475569; min-height: 1.2em; }
.skiplist-controls { display: flex; gap: 6px; flex-wrap: wrap; }
```

- [ ] **Step 10: Run the test to verify it passes**

Run: `npx playwright test -g "Skip List renders"`
Expected: PASS.

- [ ] **Step 11: Commit**

```bash
git add app.js style.css tests/visualizer.spec.js
git commit -m "feat: skip-list method + shared step-controls helper"
```

---

## Task 4: count-min-sketch method

**Files:**
- Modify: `app.js` — `METHOD_GROUPS` `hash` group, `getCodeForMethod`, `updateLayout`, `renderAll`, new `renderCountMinSketch()`
- Modify: `style.css` — append `cms-` rules
- Test: `tests/visualizer.spec.js`

- [ ] **Step 1: Write the failing Playwright test**

Add after the `Skip List renders` test:

```js
    test('Probabilistic: Count-Min Sketch renders a 3x8 grid and estimates frequency', async ({ page }) => {
        await loadMethod(page, 'count-min-sketch');
        const card = page.locator('[data-method-section="count-min-sketch"]');
        await expect(card.locator('.code-panel-filename')).toContainText('count_min_sketch.cpp');
        await expect(card.locator('.cms-cell')).toHaveCount(24);
        await card.locator('[data-cms-val]').fill('apple');
        await card.locator('[data-action="cms-add"]').click();
        await card.locator('[data-action="cms-add"]').click();
        await card.locator('[data-action="cms-estimate"]').click();
        await expect(card.locator('[data-testid="cms-readout"]')).toContainText('estimate');
        await expect(card.locator('.cms-cell.cms-hit')).toHaveCount(3);
    });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx playwright test -g "Count-Min Sketch renders"`
Expected: FAIL — `Method count-min-sketch not found in method dropdown`.

- [ ] **Step 3: Add the `METHOD_GROUPS` entry**

In `app.js`, the `hash` group's `methods` array now ends with the `skip-list` line (added in Task 3). Append one entry:

```js
            { id: 'skip-list', title: 'Skip List', file: 'skip_list.cpp', visualizer: 'skiplist', controls: 'skiplist' },
            { id: 'count-min-sketch', title: 'Count-Min Sketch', file: 'count_min_sketch.cpp', visualizer: 'cms', controls: 'cms' },
```

- [ ] **Step 4: Add the `getCodeForMethod` mapping**

In `app.js`, after `'skip-list': codeSkipList,`, add:

```js
        'skip-list': codeSkipList,
        'count-min-sketch': codeCountMinSketch,
```

- [ ] **Step 5: Add the `updateLayout` branch**

In `app.js`, after the `skip-list` branch added in Task 3, insert:

```js
        else if (currentMode === 'count-min-sketch') {
            codeTitle.textContent = 'count_min_sketch.cpp';
            codeDisplay.textContent = codeCountMinSketch;
        }
```

- [ ] **Step 6: Add the `renderAll` dispatch**

In `app.js` `renderAll()`, after the `skip-list` dispatch line added in Task 3, insert:

```js
        else if (currentMode === 'count-min-sketch') renderCountMinSketch();
```

- [ ] **Step 7: Add the `renderCountMinSketch` function**

In `app.js`, immediately after the `renderSkipList()` function, add:

```js
    function renderCountMinSketch() {
        const host = acquireDynamicVizHost();
        const DEPTH = 3, WIDTH = 8;
        if (!runtimeVisualizer._cms) {
            runtimeVisualizer._cms = {
                table: Array.from({ length: DEPTH }, () => new Array(WIDTH).fill(0)),
                actual: {},
            };
        }
        const cms = runtimeVisualizer._cms;
        function hash(row, s) {
            let h = ((row + 1) * 2654435761) >>> 0;
            for (const c of s) h = (h * 31 + c.charCodeAt(0)) >>> 0;
            return h % WIDTH;
        }

        const wrap = document.createElement('div');
        wrap.className = 'cms-wrap';
        let html = '<div class="cms-grid">';
        for (let r = 0; r < DEPTH; r++) {
            html += '<div class="cms-rowlabel">h' + r + '</div>';
            for (let c = 0; c < WIDTH; c++) {
                html += '<span class="cms-cell" data-row="' + r + '" data-col="' + c + '">' +
                        cms.table[r][c] + '</span>';
            }
        }
        html += '</div>';
        html += '<div class="cms-readout" data-testid="cms-readout">&nbsp;</div>';
        html += '<div class="cms-controls" role="group">' +
                    '<input type="text" value="apple" data-cms-val>' +
                    '<button type="button" data-action="cms-add">Add</button>' +
                    '<button type="button" data-action="cms-estimate">Estimate</button>' +
                '</div>';
        wrap.innerHTML = html;
        host.appendChild(wrap);

        const valInput = wrap.querySelector('[data-cms-val]');
        const readoutEl = wrap.querySelector('.cms-readout');
        function highlight(cells) {
            wrap.querySelectorAll('.cms-cell').forEach((c) => c.classList.remove('cms-hit'));
            for (const rc of cells) {
                const el = wrap.querySelector('.cms-cell[data-row="' + rc[0] + '"][data-col="' + rc[1] + '"]');
                if (el) el.classList.add('cms-hit');
            }
        }
        wrap.querySelector('[data-action="cms-add"]').onclick = () => {
            const key = valInput.value.trim();
            if (!key) { showStatus('Enter a word', '#f87171'); return; }
            const cells = [];
            for (let r = 0; r < DEPTH; r++) {
                const c = hash(r, key);
                cms.table[r][c]++;
                cells.push([r, c]);
                const el = wrap.querySelector('.cms-cell[data-row="' + r + '"][data-col="' + c + '"]');
                if (el) el.textContent = cms.table[r][c];
            }
            cms.actual[key] = (cms.actual[key] || 0) + 1;
            highlight(cells);
            showStatus('Added "' + key + '" (+1 per row)', '#34d399');
        };
        wrap.querySelector('[data-action="cms-estimate"]').onclick = () => {
            const key = valInput.value.trim();
            if (!key) { showStatus('Enter a word', '#f87171'); return; }
            const cells = [], vals = [];
            for (let r = 0; r < DEPTH; r++) {
                const c = hash(r, key);
                cells.push([r, c]);
                vals.push(cms.table[r][c]);
            }
            highlight(cells);
            const est = Math.min.apply(null, vals);
            const actual = cms.actual[key] || 0;
            readoutEl.textContent = 'estimate("' + key + '") = min(' + vals.join(', ') + ') = ' + est +
                                    '  |  actual = ' + actual;
            showStatus('Estimate ' + est + ' (actual ' + actual + ')', '#f59e0b');
        };
    }
```

- [ ] **Step 8: Append the `cms-` CSS**

At the end of `style.css`, append:

```css
/* Spec 3 — Count-Min Sketch */
.cms-wrap { display: flex; flex-direction: column; gap: 10px; align-items: center; }
.cms-grid { display: grid; grid-template-columns: auto repeat(8, 32px); gap: 3px; }
.cms-rowlabel { display: flex; align-items: center; font-family: monospace; font-size: 12px; color: #64748b; }
.cms-cell { width: 32px; height: 30px; display: flex; align-items: center; justify-content: center;
  border: 1px solid #cbd5e1; border-radius: 3px; font-family: monospace; background: #f8fafc; }
.cms-cell.cms-hit { background: #fde68a; border-color: #f59e0b; font-weight: 700; }
.cms-readout { font-family: monospace; font-size: 13px; color: #475569; min-height: 1.2em; }
.cms-controls { display: flex; gap: 6px; flex-wrap: wrap; justify-content: center; }
```

- [ ] **Step 9: Run the test to verify it passes**

Run: `npx playwright test -g "Count-Min Sketch renders"`
Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add app.js style.css tests/visualizer.spec.js
git commit -m "feat: count-min-sketch probabilistic method"
```

---

## Task 5: search-zalgo method

**Files:**
- Modify: `app.js` — `METHOD_GROUPS` `searching` group, `getCodeForMethod`, `updateLayout`, `renderAll`, new `renderZAlgo()`
- Modify: `style.css` — append `zalgo-` rules
- Test: `tests/visualizer.spec.js`

- [ ] **Step 1: Write the failing Playwright test**

Add after the `Count-Min Sketch renders` test:

```js
    test('String: Z-Algorithm renders the concatenated string with a Z-array and steps', async ({ page }) => {
        await loadMethod(page, 'search-zalgo');
        const card = page.locator('[data-method-section="search-zalgo"]');
        await expect(card.locator('.code-panel-filename')).toContainText('search_zalgo.cpp');
        await expect(card.locator('.zalgo-chr .zalgo-cell')).toHaveCount(29);
        await card.locator('[data-action="step"]').click();
        await expect(card.locator('[data-testid="zalgo-stats"]')).toContainText('computed: 1');
    });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx playwright test -g "Z-Algorithm renders"`
Expected: FAIL — `Method search-zalgo not found in method dropdown`.

- [ ] **Step 3: Add the `METHOD_GROUPS` entry**

In `app.js`, the `searching` group (`id: 'searching'`) ends its `methods` array with the `search-strcompare` line (`app.js:187`). Append one entry:

```js
            { id: 'search-strcompare', title: 'String Matching Compared', file: 'search_strcompare.cpp', visualizer: 'string-compare', controls: 'string-compare' },
            { id: 'search-zalgo', title: 'Z-Algorithm', file: 'search_zalgo.cpp', visualizer: 'string-search', controls: 'string-search' },
```

(`search-aho` is appended by Task 6.)

- [ ] **Step 4: Add the `getCodeForMethod` mapping**

In `app.js`, after `'search-strcompare': codeSearchStrCompare,` (`app.js:289`), add:

```js
        'search-strcompare': codeSearchStrCompare,
        'search-zalgo': codeSearchZAlgo,
```

- [ ] **Step 5: Add the `updateLayout` branch**

In `app.js`, after the `search-strcompare` branch (`app.js:1776-1779`, which ends `}`), insert:

```js
        else if (currentMode === 'search-zalgo') {
            codeTitle.textContent = 'search_zalgo.cpp';
            codeDisplay.textContent = codeSearchZAlgo;
        }
```

- [ ] **Step 6: Add the `renderAll` dispatch**

In `app.js` `renderAll()`, after `else if (currentMode === 'search-strcompare') renderStringCompare();` (`app.js:1952`) and **before** `else if (currentMode.includes('search'))` (`app.js:1953`), insert:

```js
        else if (currentMode === 'search-zalgo') renderZAlgo();
```

This must precede the `currentMode.includes('search')` catch-all, or `search-zalgo` falls into `renderSearchArray`.

- [ ] **Step 7: Add the `renderZAlgo` function**

In `app.js`, immediately after the `renderCountMinSketch()` function, add:

```js
    function renderZAlgo() {
        const host = acquireDynamicVizHost();
        const pattern = 'ABABCABAB';
        const text = 'ABABDABACDABABCABAB';
        const s = pattern + '$' + text;
        const n = s.length, m = pattern.length;
        const z = new Array(n).fill(0);
        const trace = [];
        (function () {
            let l = 0, r = 0;
            for (let i = 1; i < n; i++) {
                if (i < r) z[i] = Math.min(r - i, z[i - l]);
                while (i + z[i] < n && s[z[i]] === s[i + z[i]]) z[i]++;
                if (i + z[i] > r) { l = i; r = i + z[i]; }
                trace[i] = { l: l, r: r };
            }
        })();

        let cur = 1;  // next index to reveal

        const wrap = document.createElement('div');
        wrap.className = 'zalgo-wrap';
        wrap.innerHTML =
            '<div class="zalgo-grid"></div>' +
            '<div class="zalgo-stats" data-testid="zalgo-stats">computed: <span class="zalgo-count">0</span>' +
                ' &nbsp;|&nbsp; matches: <span class="zalgo-matches">[]</span></div>';
        host.appendChild(wrap);
        const gridEl = wrap.querySelector('.zalgo-grid');
        const countEl = wrap.querySelector('.zalgo-count');
        const matchesEl = wrap.querySelector('.zalgo-matches');

        function draw() {
            const box = cur > 1 ? trace[cur - 1] : { l: 0, r: 0 };
            let chr = '<div class="zalgo-row zalgo-chr">';
            let zr = '<div class="zalgo-row zalgo-z">';
            const matches = [];
            for (let k = 0; k < n; k++) {
                const inBox = box.r > box.l && k >= box.l && k < box.r;
                chr += '<span class="zalgo-cell' + (inBox ? ' zalgo-box' : '') +
                       (k === cur && cur < n ? ' zalgo-cur' : '') + '">' + s[k] + '</span>';
                let zval = '-';
                if (k > 0 && k < cur) {
                    zval = z[k];
                    if (z[k] === m) matches.push(k - m - 1);
                } else if (k >= cur) {
                    zval = '?';
                }
                zr += '<span class="zalgo-cell' + (k < cur && k > 0 && z[k] === m ? ' zalgo-match' : '') +
                      '">' + zval + '</span>';
            }
            chr += '</div>';
            zr += '</div>';
            gridEl.innerHTML = chr + zr;
            countEl.textContent = Math.max(0, cur - 1);
            matchesEl.textContent = '[' + matches.join(',') + ']';
        }
        function step() {
            if (cur >= n) return false;
            cur++;
            draw();
            return cur < n;
        }
        function reset() { cur = 1; draw(); }
        wrap.appendChild(buildStepControls(step, reset, 350));
        draw();
    }
```

- [ ] **Step 8: Append the `zalgo-` CSS**

At the end of `style.css`, append:

```css
/* Spec 3 — Z-Algorithm */
.zalgo-wrap { display: flex; flex-direction: column; gap: 8px; }
.zalgo-grid { display: flex; flex-direction: column; gap: 2px; overflow-x: auto; }
.zalgo-row { display: flex; gap: 2px; }
.zalgo-cell { min-width: 20px; height: 24px; display: flex; align-items: center; justify-content: center;
  border: 1px solid #cbd5e1; font-family: monospace; font-size: 12px; background: #f8fafc; }
.zalgo-z .zalgo-cell { background: #fff; color: #475569; }
.zalgo-cell.zalgo-box { background: #e0e7ff; }
.zalgo-cell.zalgo-cur { border-color: #f59e0b; border-width: 2px; }
.zalgo-cell.zalgo-match { background: #bbf7d0; border-color: #22c55e; font-weight: 700; }
.zalgo-stats { font-family: monospace; font-size: 13px; color: #475569; }
```

- [ ] **Step 9: Run the test to verify it passes**

Run: `npx playwright test -g "Z-Algorithm renders"`
Expected: PASS.

- [ ] **Step 10: Commit**

```bash
git add app.js style.css tests/visualizer.spec.js
git commit -m "feat: search-zalgo string-matching method"
```

---

## Task 6: search-aho method + regression-test extension

**Files:**
- Modify: `app.js` — `METHOD_GROUPS` `searching` group, `getCodeForMethod`, `updateLayout`, `renderAll`, new `renderAhoCorasick()`
- Modify: `style.css` — append `aho-` rules
- Test: `tests/visualizer.spec.js` — new test + extend the existing regression test

- [ ] **Step 1: Write the failing Playwright test**

Add after the `Z-Algorithm renders` test:

```js
    test('String: Aho-Corasick renders the trie and steps through build + scan', async ({ page }) => {
        await loadMethod(page, 'search-aho');
        const card = page.locator('[data-method-section="search-aho"]');
        await expect(card.locator('.code-panel-filename')).toContainText('search_aho.cpp');
        await expect(card.locator('.aho-svg circle')).toHaveCount(10);
        await expect(card.locator('[data-testid="aho-phase"]')).toContainText('Phase 1');
        await card.locator('[data-action="step"]').click();
        await expect(card.locator('[data-testid="aho-phase"]')).toBeVisible();
    });
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx playwright test -g "Aho-Corasick renders"`
Expected: FAIL — `Method search-aho not found in method dropdown`.

- [ ] **Step 3: Add the `METHOD_GROUPS` entry**

In `app.js`, the `searching` group's `methods` array now ends with the `search-zalgo` line (added in Task 5). Append one entry:

```js
            { id: 'search-zalgo', title: 'Z-Algorithm', file: 'search_zalgo.cpp', visualizer: 'string-search', controls: 'string-search' },
            { id: 'search-aho', title: 'Aho-Corasick', file: 'search_aho.cpp', visualizer: 'aho-corasick', controls: 'aho-corasick' },
```

- [ ] **Step 4: Add the `getCodeForMethod` mapping**

In `app.js`, after `'search-zalgo': codeSearchZAlgo,`, add:

```js
        'search-zalgo': codeSearchZAlgo,
        'search-aho': codeSearchAho,
```

- [ ] **Step 5: Add the `updateLayout` branch**

In `app.js`, after the `search-zalgo` branch added in Task 5, insert:

```js
        else if (currentMode === 'search-aho') {
            codeTitle.textContent = 'search_aho.cpp';
            codeDisplay.textContent = codeSearchAho;
        }
```

- [ ] **Step 6: Add the `renderAll` dispatch**

In `app.js` `renderAll()`, after the `search-zalgo` dispatch line added in Task 5 and **before** `else if (currentMode.includes('search'))`, insert:

```js
        else if (currentMode === 'search-aho') renderAhoCorasick();
```

- [ ] **Step 7: Add the `renderAhoCorasick` function**

In `app.js`, immediately after the `renderZAlgo()` function, add:

```js
    function renderAhoCorasick() {
        const host = acquireDynamicVizHost();
        // Fixed trie for patterns {he, she, his, hers}.
        const nodes = [
            { id: 0, ch: '', x: 180, y: 30, parent: -1 },
            { id: 1, ch: 'h', x: 110, y: 95, parent: 0 },
            { id: 2, ch: 'e', x: 70, y: 160, parent: 1 },
            { id: 3, ch: 'r', x: 70, y: 225, parent: 2 },
            { id: 4, ch: 's', x: 70, y: 290, parent: 3 },
            { id: 5, ch: 'i', x: 150, y: 160, parent: 1 },
            { id: 6, ch: 's', x: 150, y: 225, parent: 5 },
            { id: 7, ch: 's', x: 250, y: 95, parent: 0 },
            { id: 8, ch: 'h', x: 250, y: 160, parent: 7 },
            { id: 9, ch: 'e', x: 250, y: 225, parent: 8 },
        ];
        const output = { 2: 'he', 4: 'hers', 6: 'his', 9: 'she' };
        // Failure links in BFS computation order (one per non-root node).
        const failSteps = [
            { node: 1, fail: 0 }, { node: 7, fail: 0 }, { node: 2, fail: 0 }, { node: 5, fail: 0 },
            { node: 8, fail: 1 }, { node: 3, fail: 0 }, { node: 6, fail: 7 }, { node: 9, fail: 2 },
            { node: 4, fail: 7 },
        ];
        const text = 'ushers';
        // Scan trace: automaton node after reading each char, and matches reported.
        const scanSteps = [
            { node: 0, matches: [] },
            { node: 7, matches: [] },
            { node: 8, matches: [] },
            { node: 9, matches: ['she@1', 'he@2'] },
            { node: 3, matches: [] },
            { node: 4, matches: ['hers@2'] },
        ];
        const TOTAL = failSteps.length + scanSteps.length;
        let idx = 0;

        const wrap = document.createElement('div');
        wrap.className = 'aho-wrap';
        wrap.innerHTML =
            '<div class="aho-phase" data-testid="aho-phase"></div>' +
            '<svg class="aho-svg" viewBox="0 0 320 320" width="100%" xmlns="http://www.w3.org/2000/svg"></svg>' +
            '<div class="aho-textrow"></div>' +
            '<div class="aho-stats" data-testid="aho-stats">matches: <span class="aho-matches">[]</span></div>';
        host.appendChild(wrap);
        const svgEl = wrap.querySelector('.aho-svg');
        const phaseEl = wrap.querySelector('.aho-phase');
        const textRowEl = wrap.querySelector('.aho-textrow');
        const matchesEl = wrap.querySelector('.aho-matches');

        function nodeById(id) { return nodes.find((n) => n.id === id); }

        function draw() {
            const inBuild = idx < failSteps.length;
            const builtCount = inBuild ? idx : failSteps.length;
            let curScanNode = -1;
            let allMatches = [];
            if (!inBuild) {
                const sIdx = idx - failSteps.length;
                for (let k = 0; k <= sIdx && k < scanSteps.length; k++) {
                    curScanNode = scanSteps[k].node;
                    allMatches = allMatches.concat(scanSteps[k].matches);
                }
            }
            let svg = '';
            for (const n of nodes) {
                if (n.parent >= 0) {
                    const p = nodeById(n.parent);
                    svg += '<line x1="' + p.x + '" y1="' + p.y + '" x2="' + n.x + '" y2="' + n.y +
                           '" stroke="#94a3b8" stroke-width="2"/>';
                }
            }
            for (let k = 0; k < builtCount; k++) {
                const fs = failSteps[k];
                const a = nodeById(fs.node), b = nodeById(fs.fail);
                svg += '<line x1="' + a.x + '" y1="' + a.y + '" x2="' + b.x + '" y2="' + b.y +
                       '" stroke="#f59e0b" stroke-width="1.5" stroke-dasharray="4 3"/>';
            }
            for (const n of nodes) {
                const isBuildCur = inBuild && failSteps[idx] && n.id === failSteps[idx].node;
                const isCur = (!inBuild && n.id === curScanNode) || isBuildCur;
                const hasOut = output[n.id] !== undefined;
                svg += '<circle cx="' + n.x + '" cy="' + n.y + '" r="16" fill="' +
                       (isCur ? '#34d399' : (hasOut ? '#dbeafe' : '#fff')) +
                       '" stroke="#1e40af" stroke-width="2" data-node="' + n.id + '"/>';
                svg += '<text x="' + n.x + '" y="' + (n.y + 5) + '" text-anchor="middle" font-size="13" ' +
                       'font-weight="700">' + (n.ch || '·') + '</text>';
            }
            svgEl.innerHTML = svg;

            const scanPos = inBuild ? -1 : (idx - failSteps.length);
            let tr = '';
            for (let k = 0; k < text.length; k++) {
                tr += '<span class="aho-char' + (k === scanPos ? ' aho-char-cur' : '') + '">' + text[k] + '</span>';
            }
            textRowEl.innerHTML = tr;

            phaseEl.textContent = inBuild
                ? 'Phase 1: Building failure links (' + builtCount + '/' + failSteps.length + ')'
                : 'Phase 2: Scanning text';
            matchesEl.textContent = '[' + allMatches.join(', ') + ']';
        }
        function step() {
            if (idx >= TOTAL) return false;
            idx++;
            draw();
            return idx < TOTAL;
        }
        function reset() { idx = 0; draw(); }
        wrap.appendChild(buildStepControls(step, reset, 500));
        draw();
    }
```

- [ ] **Step 8: Append the `aho-` CSS**

At the end of `style.css`, append:

```css
/* Spec 3 — Aho-Corasick */
.aho-wrap { display: flex; flex-direction: column; gap: 8px; align-items: center; }
.aho-phase { font-size: 13px; font-weight: 600; color: #1e40af; }
.aho-svg { max-width: 340px; }
.aho-textrow { display: flex; gap: 4px; }
.aho-char { width: 26px; height: 30px; display: flex; align-items: center; justify-content: center;
  border: 1px solid #cbd5e1; border-radius: 3px; font-family: monospace; font-weight: 700; background: #f8fafc; }
.aho-char.aho-char-cur { background: #34d399; border-color: #059669; color: #fff; }
.aho-stats { font-family: monospace; font-size: 13px; color: #475569; }
```

- [ ] **Step 9: Run the test to verify it passes**

Run: `npx playwright test -g "Aho-Corasick renders"`
Expected: PASS.

- [ ] **Step 10: Extend the dynamic→static regression test**

In `tests/visualizer.spec.js`, the test `Navigation: switching from Spec-2a dynamic visualizers back to static ones does not crash` ends with:

```js
        await loadMethod(page, 'search-kmp');
        await loadMethod(page, 'search-linear');
        await expect(page.locator('#search-container')).toHaveCount(1);
        expect(errors).toEqual([]);
```

Insert two new navigation paths immediately before `expect(errors).toEqual([]);`:

```js
        await loadMethod(page, 'bloom-filter');
        await loadMethod(page, 'hash-chain');
        await expect(page.locator('#hash-ch-container')).toHaveCount(1);
        await loadMethod(page, 'search-zalgo');
        await loadMethod(page, 'search-aho');
        await loadMethod(page, 'search-linear');
        await expect(page.locator('#search-container')).toHaveCount(1);
        expect(errors).toEqual([]);
```

- [ ] **Step 11: Run the regression test to verify it passes**

Run: `npx playwright test -g "does not crash"`
Expected: PASS.

- [ ] **Step 12: Run the full Playwright suite**

Run: `npm test`
Expected: 81 passed (76 existing + 5 new).

- [ ] **Step 13: Commit**

```bash
git add app.js style.css tests/visualizer.spec.js
git commit -m "feat: search-aho method + extend dynamic-to-static regression test"
```

---

## Task 7: bloom-filter reference slide deck

**Files:**
- Modify: `slides_db.js` — add the `"bloom-filter"` deck
- Regenerate: `slides_rendered.js`, `slides/`

This task authors one complete deck verbatim as the reference; Task 8 authors the other four with the same structure.

- [ ] **Step 1: Add the `bloom-filter` deck to `slides_db.js`**

Insert this entry near the other method decks (object key order does not affect generation):

```js
  "bloom-filter": {
    "category": "Hash & Probabilistic",
    "title": { "zh": "布隆過濾器", "en": "Bloom Filter" },
    "slides": [
      {
        "heading": { "zh": "布隆過濾器", "en": "Bloom Filter" },
        "blocks": [
          {
            "type": "paragraph",
            "text": {
              "zh": "布隆過濾器是一種空間效率極高的機率型集合,用來測試「某元素是否屬於集合」。它可能回報誤判存在(false positive),但絕不會漏判(false negative)。",
              "en": "A Bloom filter is a highly space-efficient probabilistic set used to test set membership. It may report a false positive but never a false negative."
            }
          }
        ]
      },
      {
        "heading": { "zh": "核心概念", "en": "Core Concept" },
        "blocks": [
          {
            "type": "paragraph",
            "text": {
              "zh": "結構是一個 $m$ 位元的位元陣列加上 $k$ 個獨立雜湊函式。插入元素時把 $k$ 個雜湊位置設為 1;查詢時檢查那 $k$ 個位置是否全為 1。",
              "en": "The structure is an $m$-bit array plus $k$ independent hash functions. Insertion sets the $k$ hashed bits to 1; a query checks whether those $k$ bits are all 1."
            }
          },
          {
            "type": "bullets",
            "items": [
              { "zh": "$k$ 個位元全為 1 → 「可能存在」,也可能是其他元素恰好湊齊。", "en": "All $k$ bits are 1 → \"possibly present\" — other elements may have set those bits." },
              { "zh": "任一位元為 0 → 「絕對不存在」。", "en": "Any bit is 0 → \"definitely not present\"." },
              { "zh": "不支援刪除:清除位元會誤傷共用該位元的其他元素。", "en": "No deletion: clearing a bit would corrupt other elements sharing it." }
            ]
          }
        ]
      },
      {
        "heading": { "zh": "運作流程", "en": "Operation Flow" },
        "blocks": [
          {
            "type": "steps",
            "items": [
              { "zh": "插入 $x$:計算 $h_1(x)$、$h_2(x)$、$h_3(x)$,把這 3 個位元設為 1。", "en": "Insert $x$: compute $h_1(x)$, $h_2(x)$, $h_3(x)$ and set those 3 bits to 1." },
              { "zh": "查詢 $y$:計算 $y$ 的 3 個雜湊位置。", "en": "Query $y$: compute the 3 hashed positions of $y$." },
              { "zh": "3 個位元全為 1 回報「可能存在」;任一為 0 回報「絕對不存在」。", "en": "All 3 bits 1 → \"possibly present\"; any bit 0 → \"definitely not present\"." }
            ]
          },
          {
            "type": "mermaid",
            "code": "flowchart TD\n  A[\"query y\"] --> B[\"compute h1 h2 h3\"]\n  B --> C[\"all 3 bits = 1?\"]\n  C --> D[\"possibly present\"]\n  C --> E[\"definitely not present\"]"
          }
        ]
      },
      {
        "heading": { "zh": "示意圖", "en": "Layout" },
        "blocks": [
          {
            "type": "svg",
            "svg": "<svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 360 70\" width=\"360\"><g font-family=\"monospace\" font-size=\"12\"><text x=\"0\" y=\"16\">insert \"dog\" sets bits 4, 17, 29</text><rect x=\"0\" y=\"28\" width=\"352\" height=\"24\" fill=\"none\" stroke=\"#cbd5e1\"/><rect x=\"44\" y=\"28\" width=\"22\" height=\"24\" fill=\"#dbeafe\" stroke=\"#3b82f6\"/><rect x=\"187\" y=\"28\" width=\"22\" height=\"24\" fill=\"#dbeafe\" stroke=\"#3b82f6\"/><rect x=\"319\" y=\"28\" width=\"22\" height=\"24\" fill=\"#dbeafe\" stroke=\"#3b82f6\"/></g></svg>"
          },
          {
            "type": "note",
            "text": {
              "zh": "visualizer 以 32 格位元陣列呈現;Insert 把 3 個雜湊位元點亮,Query 高亮被檢查的 3 格並回報結果。",
              "en": "The visualizer shows a 32-cell bit array; Insert lights up the 3 hashed bits, Query highlights the 3 checked cells and reports the verdict."
            }
          }
        ]
      },
      {
        "heading": { "zh": "複雜度分析", "en": "Complexity Analysis" },
        "blocks": [
          {
            "type": "table",
            "headers": [
              { "zh": "項目", "en": "Aspect" },
              { "zh": "複雜度", "en": "Complexity" }
            ],
            "rows": [
              [ { "zh": "插入 / 查詢時間", "en": "insert / query time" }, { "zh": "$O(k)$", "en": "$O(k)$" } ],
              [ { "zh": "空間", "en": "space" }, { "zh": "$O(m)$ 位元", "en": "$O(m)$ bits" } ],
              [ { "zh": "漏判(false negative)", "en": "false negative" }, { "zh": "不可能", "en": "impossible" } ]
            ]
          },
          {
            "type": "math",
            "tex": "P_{\\text{fp}} \\approx \\left(1 - e^{-kn/m}\\right)^k",
            "caption": {
              "zh": "誤判率隨已插入元素數 $n$ 上升;在固定 $m$ 下存在最佳的 $k$。",
              "en": "The false-positive rate rises with the number of inserted elements $n$; for a fixed $m$ there is an optimal $k$."
            }
          }
        ]
      },
      {
        "heading": { "zh": "程式碼", "en": "Source Code" },
        "blocks": [
          {
            "type": "code",
            "lang": "cpp",
            "code": "void insert(const std::string& key) {\n    bits[hash1(key)] = true;\n    bits[hash2(key)] = true;\n    bits[hash3(key)] = true;\n}\n\nbool possiblyContains(const std::string& key) const {\n    return bits[hash1(key)] && bits[hash2(key)] && bits[hash3(key)];\n}"
          }
        ]
      },
      {
        "heading": { "zh": "優缺點與使用時機", "en": "Pros, Cons & When to Use" },
        "blocks": [
          {
            "type": "bullets",
            "items": [
              { "zh": "優點:空間遠小於儲存完整元素集合。", "en": "Pro: uses far less space than storing the full element set." },
              { "zh": "優點:插入與查詢皆為固定時間 $O(k)$。", "en": "Pro: insertion and query are both constant-time $O(k)$." },
              { "zh": "缺點:有誤判,且無法刪除元素。", "en": "Con: false positives occur, and elements cannot be removed." },
              { "zh": "適用:快取前置過濾、去重、判斷「絕對沒看過」的場景。", "en": "Use for cache pre-filtering, deduplication, and \"definitely never seen\" checks." }
            ]
          }
        ]
      },
      {
        "heading": { "zh": "小結", "en": "Summary" },
        "blocks": [
          {
            "type": "bullets",
            "items": [
              { "zh": "位元陣列加多個雜湊函式構成的機率型集合。", "en": "A probabilistic set built from a bit array plus several hash functions." },
              { "zh": "「絕對不存在」可信;「可能存在」需容忍誤判。", "en": "\"Definitely absent\" is certain; \"possibly present\" must tolerate false positives." },
              { "zh": "以準確度換取極小的空間佔用。", "en": "Trades accuracy for a very small space footprint." }
            ]
          }
        ]
      }
    ]
  },
```

- [ ] **Step 2: Regenerate slides**

Run: `npm run build:slides`
Expected: `Generated NN decks for zh, en` (deck count rises by 1); `slides/zh/bloom-filter.md` and `slides/en/bloom-filter.md` created.

- [ ] **Step 3: Verify the deck renders**

Run: `node -e "const d=require('./slides_rendered.js'); const k=Object.keys(d).find(x=>x.includes('bloom')); console.log(k ? 'bloom deck present' : 'MISSING');"`
Expected: `bloom deck present` (if `slides_rendered.js` is not directly requireable, instead confirm `slides/zh/bloom-filter.md` exists and has 8 `---` slide separators with `grep -c '^---$' slides/zh/bloom-filter.md`).

- [ ] **Step 4: Commit**

```bash
git add slides_db.js slides_rendered.js slides
git commit -m "feat: author bloom-filter reference slide deck"
```

---

## Task 8: skip-list, count-min-sketch, search-zalgo, search-aho slide decks

**Files:**
- Modify: `slides_db.js` — add 4 decks
- Regenerate: `slides_rendered.js`, `slides/`

Author 4 decks with the **exact same 8-slide structure** as the `bloom-filter` reference deck from Task 7 (概述 paragraph / 核心概念 paragraph+bullets / 運作流程 steps+mermaid / 示意圖 svg+note / 複雜度分析 table+math / 程式碼 code / 優缺點 bullets / 小結 bullets). All decks: bilingual `zh`+`en`, full-width Chinese punctuation, mermaid `flowchart` with rectangular `[...]` nodes only, LaTeX backslashes written `\\` in JS strings.

- [ ] **Step 1: Add the `skip-list` deck**

`category: "Hash & Probabilistic"`, title zh `跳躍列表` / en `Skip List`. Content per slide:
1. **概述** — 跳躍列表是一種以多層連結串列實作的有序映射;靠隨機層級提供期望 $O(\log n)$ 的搜尋、插入、刪除,是平衡樹的簡單替代。
2. **核心概念** — paragraph: level 0 含全部節點,上層為「快車道」,搜尋時由高層往低層下降。bullets: 每個節點以擲硬幣決定層數;期望每節點約 2 個指標;不需旋轉即維持平衡(機率上)。
3. **運作流程** — steps: 從最高層的 head 出發;當下一個節點鍵值 < 目標就往右,否則下降一層;在 level 0 檢查下一個節點是否等於目標。mermaid: `flowchart LR\n  S["start at top level"] --> R["next.key < target?"]\n  R --> M["move right"]\n  R --> D["drop one level"]`.
4. **示意圖** — svg: 一個小型多層串列示意(可用文字行 `L0 head -> 3 -> 7 -> 12 -> NIL` 疊兩三層)。note: visualizer 以垂直堆疊的 4 層呈現,Insert 擲硬幣決定層數,Search 逐步動畫呈現下降路徑。
5. **複雜度分析** — table(操作 / 期望 / 最差):search、insert、delete 期望 $O(\log n)$、最差 $O(n)$;空間 $O(n)$。math: `T_{\\text{search}}(n) = O(\\log n)`,caption 說明期望值來自隨機層級。
6. **程式碼** — code(cpp):`search()` 函式的精簡版(逐層下降的雙重迴圈)。
7. **優缺點** — 優點:實作比平衡樹簡單、支援有序走訪;缺點:最差情況退化、依賴亂數品質;適用:需要有序映射又想避開紅黑樹複雜度的場合。
8. **小結** — 多層快車道串列;隨機層級取代旋轉維持平衡;期望 $O(\log n)$。

- [ ] **Step 2: Add the `count-min-sketch` deck**

`category: "Hash & Probabilistic"`, title zh `Count-Min Sketch` / en `Count-Min Sketch`. Content per slide:
1. **概述** — Count-Min Sketch 是一種機率型頻率表,用極小空間估計每個元素的出現次數;估計值永不低估,但可能因雜湊碰撞而高估。
2. **核心概念** — paragraph: 一個 $d \times w$ 的計數矩陣,每列搭配一個雜湊函式。bullets: update 對每列各加 1;estimate 取 $d$ 個對應格的最小值;碰撞只會讓計數偏高,故取 min 最接近真值。
3. **運作流程** — steps: update($x$) 對每一列 $r$ 計算 $h_r(x)$ 並把該格 +1;estimate($x$) 讀出 $d$ 個格;回傳這些值的最小值。mermaid: `flowchart LR\n  U["update x"] --> H["d row hashes"]\n  H --> I["increment d cells"]\n  E["estimate x"] --> M["take min of d cells"]`.
4. **示意圖** — svg: 一個 3×8 的小格矩陣示意,標出某元素命中的 3 格。note: visualizer 以 3×8 計數矩陣呈現,Add 高亮遞增的 3 格,Estimate 顯示 min 與真實計數對照。
5. **複雜度分析** — table:update / estimate 時間 $O(d)$;空間 $O(d \cdot w)$;低估不可能。math: `\\hat{f}(x) \\ge f(x)`,caption 說明估計值為上界,寬度 $w$ 越大誤差越小。
6. **程式碼** — code(cpp):`update()` 與 `estimate()`(取 min)。
7. **優缺點** — 優點:空間固定、與元素種類數無關;缺點:有高估誤差、不支援精確查詢;適用:資料流的熱門項統計、近似頻率。
8. **小結** — 二維計數矩陣 + 多雜湊;取 min 抵銷碰撞造成的高估;以誤差換取極小且固定的空間。

- [ ] **Step 3: Add the `search-zalgo` deck**

`category: "Searching & String Matching"`, title zh `Z 演算法` / en `Z-Algorithm`. Content per slide:
1. **概述** — Z 演算法以線性時間建立字串的 Z 陣列:$Z[i]$ 是從位置 $i$ 起、與字串前綴相符的最長長度;可用於 $O(n+m)$ 字串比對。
2. **核心概念** — paragraph: 維護一個 $[l, r]$ 視窗(目前已知與前綴相符的最右區段),讓區段內的索引可重用先前算過的值。bullets: $i$ 落在 $[l,r]$ 內時用鏡射值 $Z[i-l]$ 起步;落在外面時從零逐字比對;比對後更新 $[l,r]$。
3. **運作流程** — steps: 串接 `pattern + '$' + text`;對串接字串求 Z 陣列;凡 $Z[i]$ 等於 pattern 長度即為一次命中。mermaid: `flowchart LR\n  C["concat P + $ + T"] --> Z["compute Z array"]\n  Z --> F["Z[i] == |P| means match"]`.
4. **示意圖** — svg: 字元列加上一列 Z 值的示意。note: visualizer 顯示串接字串與逐步填入的 Z 陣列,並標出 $[l,r]$ 視窗與命中位置。
5. **複雜度分析** — table:建立 Z 陣列時間 $O(n)$;字串比對 $O(n+m)$;空間 $O(n+m)$。math: `T(n) = O(n)`,caption 說明 $[l,r]$ 視窗使每個字元至多被比較常數次。
6. **程式碼** — code(cpp):`computeZ()` 主迴圈。
7. **優缺點** — 優點:概念單純、線性時間、不需失敗函數表;缺點:需額外串接字串與 $O(n+m)$ 空間;適用:單模式比對、字串週期與前綴分析。
8. **小結** — Z 陣列描述「與前綴相符的長度」;$[l,r]$ 視窗達成線性時間;串接 `P$T` 即可做比對。

- [ ] **Step 4: Add the `search-aho` deck**

`category: "Searching & String Matching"`, title zh `Aho-Corasick 演算法` / en `Aho-Corasick`. Content per slide:
1. **概述** — Aho-Corasick 把多個樣式組成一棵 trie,再以 BFS 建立 failure 連結,讓單次文字掃描就能找出所有樣式的所有出現位置。
2. **核心概念** — paragraph: trie 提供 goto 邊;failure 連結指向「目前字串的最長真後綴所對應的節點」,使比對失敗時不必回溯文字。bullets: failure 連結以 BFS 由淺至深計算;output 連結沿 failure 鏈合併,確保不漏報;掃描時文字指標永不後退。
3. **運作流程** — steps: 把所有樣式插入 trie;BFS 計算每個節點的 failure 連結並合併 output;掃描文字,沿 goto 前進或沿 failure 回跳,落在 output 節點即回報命中。mermaid: `flowchart LR\n  T["build trie"] --> F["BFS failure links"]\n  F --> S["scan text once"]\n  S --> M["report all matches"]`.
4. **示意圖** — svg: 一棵小 trie 加上一條虛線 failure 連結的示意。note: visualizer 分兩個逐步階段 —— 先建立 failure 連結,再掃描文字 `ushers`。
5. **複雜度分析** — table:建構時間 $O(\\sum |P_i|)$;掃描 $O(|text| + \\#matches)$;空間 $O(\\sum |P_i| \\cdot \\sigma)$。math: `T_{\\text{scan}} = O(|text| + \\#matches)`,caption 說明文字指標不回溯故掃描為線性。
6. **程式碼** — code(cpp):`build()` 中 BFS 計算 failure 連結的片段。
7. **優缺點** — 優點:一次掃描處理多樣式、文字不回溯;缺點:需建構自動機、空間隨樣式總長與字母表成長;適用:多關鍵字過濾、入侵偵測、字典比對。
8. **小結** — trie 的 goto 加上 BFS 建立的 failure 連結;單次線性掃描找出所有樣式;是 KMP 對多樣式的推廣。

- [ ] **Step 5: Regenerate slides**

Run: `npm run build:slides`
Expected: `Generated NN decks for zh, en` (deck count rises by 4); 8 new files under `slides/zh/` and `slides/en/`.

- [ ] **Step 6: Verify each deck has 8 slides**

Run: `for d in skip-list count-min-sketch search-zalgo search-aho; do echo -n "$d: "; grep -c '^---$' slides/zh/$d.md; done`
Expected: each prints `8`.

- [ ] **Step 7: Commit**

```bash
git add slides_db.js slides_rendered.js slides
git commit -m "feat: author skip-list, count-min-sketch, zalgo, aho slide decks"
```

---

## Task 9: Final verification + idempotency

**Files:** No expected modifications beyond a possible normalization commit.

- [ ] **Step 1: Run the full test suite**

Run: `npm run test:all`
Expected: 125 passing (44 unit + 81 Playwright).

- [ ] **Step 2: Browser smoke check — the 5 new methods render without page errors**

Write this temporary file at the repo root:

```js
// /Users/skhuang/course/dsvisual/check_spec3.js
const { chromium } = require('@playwright/test');
const path = require('path');

const expected = [
  { id: 'bloom-filter',     selector: '.bloom-cell',                count: 32 },
  { id: 'skip-list',        selector: '.skiplist-level',            count: 4 },
  { id: 'count-min-sketch', selector: '.cms-cell',                  count: 24 },
  { id: 'search-zalgo',     selector: '.zalgo-chr .zalgo-cell',     count: 29 },
  { id: 'search-aho',       selector: '.aho-svg circle',            count: 10 },
];

(async () => {
  const browser = await chromium.launch();
  const page = await browser.newPage();
  const errors = [];
  page.on('pageerror', (e) => errors.push(e.message));
  await page.goto('file://' + path.resolve(process.cwd(), 'index.html'));
  await page.waitForSelector('[data-method-section]');
  for (const e of expected) {
    const cats = page.locator('[data-testid="category-nav"] .category-nav-btn');
    const count = await cats.count();
    let found = false;
    for (let i = 0; i < count && !found; i++) {
      await cats.nth(i).click();
      const sel = page.locator('[data-testid="method-select"]');
      if (await sel.locator('option[value="' + e.id + '"]').count()) {
        await sel.selectOption(e.id);
        found = true;
        break;
      }
      const tabs = page.locator('.category-subtab-row.visible .category-subtab-btn');
      const tc = await tabs.count();
      for (let t = 0; t < tc && !found; t++) {
        await tabs.nth(t).click();
        if (await sel.locator('option[value="' + e.id + '"]').count()) {
          await sel.selectOption(e.id);
          found = true;
        }
      }
    }
    await page.waitForSelector('[data-method-section="' + e.id + '"][data-runtime-state="active"]');
    const actual = await page.locator('[data-method-section="' + e.id + '"] ' + e.selector).count();
    console.log(e.id.padEnd(20), 'expected', e.count, 'actual', actual, actual === e.count ? 'OK' : 'FAIL');
  }
  console.log('errors:', errors);
  await browser.close();
})();
```

Run: `node check_spec3.js`
Then: `rm check_spec3.js`
Expected: every line prints `OK` and `errors: []`.

- [ ] **Step 3: Idempotency — normalize and verify**

Run: `npm run format:code`
Then: `git status --short`

`format:code` clang-formats the 5 `.cpp` files (and the C++ embedded in the new decks) and rebuilds slides — a diff in the 5 `.cpp` files, `code_db.js`, `slides_db.js`, `slides_rendered.js`, and `slides/` is expected on this first run. If there is a diff, verify it is formatting-only (`npm run test:all` still 125), then commit it:

```bash
git add bloom_filter.cpp skip_list.cpp count_min_sketch.cpp search_zalgo.cpp search_aho.cpp code_db.js slides_db.js slides_rendered.js slides
git commit -m "chore: normalize spec-3 sources and decks to canonical format:code output"
```

Then verify idempotency:

```bash
npm run format:code && git status --short
npm run build:slides && git status --short
```

Expected after each: only untracked files (e.g. `?? .claude/`), no modified files.

- [ ] **Step 4: Final test run**

Run: `npm run test:all`
Expected: 125 passing (44 unit + 81 Playwright).

If Steps 1-4 pass, the branch is ready. If any regression surfaces, fix it and commit.

---

## Notes for the executor

- **`build_db.js` mappings come first (Task 1 Step 1).** Without the mapping, `code_db.js` regeneration silently drops the constant and the app breaks with a `ReferenceError` (PR #57 lesson).
- **Task 2 adds all three Hash & Probabilistic `METHOD_GROUPS` entries / `getCodeForMethod` mappings / `updateLayout` branches / `renderAll` dispatch at once.** Tasks 3 and 4 then only add the `renderSkipList` / `renderCountMinSketch` functions and CSS. Likewise Task 5 wires both string methods; Task 6 only adds `renderAhoCorasick`.
- **`buildStepControls` is introduced in Task 3** (the first stepped method) and reused by Tasks 5 and 6. It is a function declaration, so placement before `renderBloomFilter` makes it visible to all later render functions.
- **All new render functions go through `acquireDynamicVizHost()`** — never `runtimeVisualizer.innerHTML = ''`, which destroys the static containers and crashes later navigation to static methods.
- **`renderAll` dispatch order matters:** `search-zalgo` / `search-aho` branches must precede the `currentMode.includes('search')` catch-all (`app.js:1953`), or they fall into `renderSearchArray`.
- **No `index.html` changes** — every visualizer embeds its own controls into `#dynamic-viz-host`.
- **Mermaid: rectangular `[...]` nodes only.** `([...])` stadium nodes render non-deterministically and break `build:slides` idempotency.
- **Slide decks: author then normalize.** Decks are authored in Tasks 7-8; Task 9 runs `format:code` to canonicalize `slides_db.js` and the embedded C++.
- **Skip List levels are random** — tests assert only the deterministic level-0 node count, never higher levels.
```
